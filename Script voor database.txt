SQL SCRIPT VOOR IN POSTGRES:

CREATE TABLE "profile" (
	"profile_id" serial NOT NULL,
	"order.count" varchar(45) NOT NULL,
	"recommendation_segment" varchar(45) NOT NULL,
	CONSTRAINT "profile_pk" PRIMARY KEY ("profile_id")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "session" (
	"session_id" serial NOT NULL,
	"has_sale" varchar(45) NOT NULL,
	"prefences" varchar(45) NOT NULL,
	"profile_id" integer NOT NULL,
	CONSTRAINT "session_pk" PRIMARY KEY ("session_id")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "preference_session" (
	"id" serial NOT NULL,
	"session_id" integer NOT NULL,
	"category_idcategory" integer NOT NULL,
	CONSTRAINT "preference_session_pk" PRIMARY KEY ("id")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "order_session" (
	"id" serial NOT NULL,
	"session_id" integer NOT NULL,
	"product_id" integer NOT NULL,
	CONSTRAINT "order_session_pk" PRIMARY KEY ("id")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "brand" (
	"brand_idBrand" serial NOT NULL,
	"brandnaam" serial(45) NOT NULL,
	CONSTRAINT "brand_pk" PRIMARY KEY ("brand_idBrand")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "product" (
	"product_id" serial NOT NULL,
	"selling_price" integer NOT NULL,
	"brand_idBrand" integer NOT NULL,
	"gender_idgender" integer NOT NULL,
	"herhaalaankoop" integer NOT NULL,
	"catergory_idcatergory" integer NOT NULL,
	CONSTRAINT "product_pk" PRIMARY KEY ("product_id")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "gender" (
	"gender_idgender" serial NOT NULL,
	"gendernaam" varchar(45) NOT NULL,
	CONSTRAINT "gender_pk" PRIMARY KEY ("gender_idgender")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "category" (
	"category_idcategory" serial NOT NULL,
	"category" varchar(45) NOT NULL,
	CONSTRAINT "category_pk" PRIMARY KEY ("category_idcategory")
) WITH (
  OIDS=FALSE
);



CREATE TABLE "subcategory" (
	"idsubcatergorieën" serial NOT NULL,
	"sub_category" varchar(45) NOT NULL,
	"sub_sub_category" varchar(45) NOT NULL,
	"category_idcategory" integer NOT NULL,
	CONSTRAINT "subcategory_pk" PRIMARY KEY ("idsubcatergorieën")
) WITH (
  OIDS=FALSE
);




ALTER TABLE "session" ADD CONSTRAINT "session_fk0" FOREIGN KEY ("profile_id") REFERENCES "profile"("profile_id");

ALTER TABLE "preference_session" ADD CONSTRAINT "preference_session_fk0" FOREIGN KEY ("session_id") REFERENCES "session"("session_id");
ALTER TABLE "preference_session" ADD CONSTRAINT "preference_session_fk1" FOREIGN KEY ("category_idcategory") REFERENCES "category"("category_idcategory");

ALTER TABLE "order_session" ADD CONSTRAINT "order_session_fk0" FOREIGN KEY ("session_id") REFERENCES "session"("session_id");
ALTER TABLE "order_session" ADD CONSTRAINT "order_session_fk1" FOREIGN KEY ("product_id") REFERENCES "product"("product_id");


ALTER TABLE "product" ADD CONSTRAINT "product_fk0" FOREIGN KEY ("brand_idBrand") REFERENCES "brand"("brand_idBrand");
ALTER TABLE "product" ADD CONSTRAINT "product_fk1" FOREIGN KEY ("gender_idgender") REFERENCES "gender"("gender_idgender");
ALTER TABLE "product" ADD CONSTRAINT "product_fk2" FOREIGN KEY ("catergory_idcatergory") REFERENCES "category"("category_idcategory");



ALTER TABLE "subcategory" ADD CONSTRAINT "subcategory_fk0" FOREIGN KEY ("category_idcategory") REFERENCES "category"("category_idcategory");
